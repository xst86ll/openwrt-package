name: Build K2P A2 Passwall (Slim)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    # 全局环境变量配置
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      DEBIAN_FRONTEND: noninteractive
      TZ: Asia/Shanghai
    defaults:
      run:
        shell: bash

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      env:
        FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    - name: 1. 初始化编译环境（补全依赖+优化网络）
      run: |
        # 更换阿里云源加速依赖安装
        sudo sed -i 's/archive.ubuntu.com/mirrors.aliyun.com/g' /etc/apt/sources.list
        # 配置公共DNS解决域名解析问题
        echo "nameserver 8.8.8.8" | sudo tee -a /etc/resolv.conf
        echo "nameserver 1.1.1.1" | sudo tee -a /etc/resolv.conf
        # 更新并安装完整依赖
        sudo apt update -o Acquire::Retries=10 -y
        sudo apt install -y build-essential clang flex bison g++ gawk \
        gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
        python3 python3-distutils rsync unzip zlib1g-dev file wget \
        autopoint libtool-bin pkg-config curl ca-certificates
        sudo apt clean
        # 清理磁盘空间
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc

    - name: 2. 克隆 ImmortalWrt 源码（稳定分支）
      run: |
        git clone --depth=1 --branch openwrt-23.05 https://github.com/immortalwrt/immortalwrt.git openwrt
        cd openwrt
        # 校验源码克隆结果
        if [ ! -f "Makefile" ]; then
          echo "❌ 源码克隆失败！"
          exit 1
        fi

    - name: 3. 安装 Passwall Feeds（多层兜底方案）
      run: |
        cd openwrt
        # 安全清理旧缓存
        [ -d "feeds/passwall" ] && rm -rf feeds/passwall
        [ -f "feeds.conf.default" ] || cp feeds.conf.default.example feeds.conf.default
        # 先删除所有旧的 passwall 源配置
        sed -i '/passwall/d' feeds.conf.default
        
        # 方案1：尝试官方源（重试3次）
        echo "src-git passwall https://github.com/xiaorouji/openwrt-passwall.git" >> feeds.conf.default
        ./scripts/feeds clean
        for i in {1..3}; do
          if ./scripts/feeds update -a -f; then
            echo "✅ 官方源 Feeds 更新成功！"
            break
          else
            echo "⚠️ 第 $i 次官方源更新失败，5秒后重试..."
            sleep 5
          fi
        done
        ./scripts/feeds install -a -f

        # 方案2：官方源失败则切换 jsdelivr 镜像源
        if [ ! -d "feeds/passwall" ]; then
          echo "🔄 官方源失败，切换到 jsdelivr 镜像源..."
          sed -i '/passwall/d' feeds.conf.default
          echo "src-git passwall https://cdn.jsdelivr.net/gh/xiaorouji/openwrt-passwall@main" >> feeds.conf.default
          ./scripts/feeds update -a -f
          ./scripts/feeds install -a -f
        fi

        # 方案3：终极兜底 - 手动克隆 Passwall 到 feeds 目录
        if [ ! -d "feeds/passwall" ]; then
          echo "🔧 镜像源也失败，手动克隆 Passwall..."
          mkdir -p feeds/passwall
          git clone --depth=1 https://github.com/xiaorouji/openwrt-passwall.git feeds/passwall
        fi

        # 最终校验：确保 passwall 目录存在
        if [ ! -d "feeds/passwall" ]; then
          echo "❌ 所有方案都失败，Passwall 安装失败！"
          exit 1
        else
          echo "✅ Passwall Feeds 安装成功！"
          ls -la feeds/passwall
        fi

    - name: 4. 配置 K2P 精简固件（适配16MB闪存）
      run: |
        cd openwrt
        cat > .config << 'EOF'
        CONFIG_TARGET_ramips=y
        CONFIG_TARGET_ramips_mt7621=y
        CONFIG_TARGET_ramips_mt7621_DEVICE_phicomm_k2p=y

        # 基础LuCI（中文界面）
        CONFIG_PACKAGE_luci=y
        CONFIG_LUCI_LANG_zh_Hans=y

        # Passwall 核心组件（解决VLESS/Xray问题）
        CONFIG_PACKAGE_luci-app-passwall=y
        CONFIG_PACKAGE_xray-core=y
        CONFIG_PACKAGE_xray-geodata=y
        CONFIG_PACKAGE_v2ray-geodata=y

        # 必要依赖库
        CONFIG_PACKAGE_dnsmasq=y
        CONFIG_PACKAGE_ca-bundle=y
        CONFIG_PACKAGE_curl=y
        CONFIG_PACKAGE_libstdcpp=y
        CONFIG_PACKAGE_zlib=y

        # 极致精简（控制固件<16MB）
        CONFIG_PACKAGE_ipv6helper=n
        CONFIG_PACKAGE_ip6tables=n
        CONFIG_PACKAGE_ppp=n
        CONFIG_PACKAGE_ppp-mod-pppoe=n
        CONFIG_PACKAGE_firewall4=n
        CONFIG_PACKAGE_wpad-mesh-openssl=n
        CONFIG_PACKAGE_wpad-basic-wolfssl=y

        # Passwall 兼容依赖（iptables）
        CONFIG_PACKAGE_firewall=y
        CONFIG_PACKAGE_iptables=y
        CONFIG_PACKAGE_iptables-mod-tproxy=y
        EOF
        make defconfig

    - name: 5. 下载编译依赖包（失败重试）
      run: |
        cd openwrt
        make download -j$(nproc) || make download -j1 V=s

    - name: 6. 编译固件（单线程稳定版）
      run: |
        cd openwrt
        # 单线程编译避免内存溢出
        make -j1 V=s 2>&1 | tee compile.log

    - name: 7. 上传编译日志（用于排查问题）
      uses: actions/upload-artifact@v4
      env:
        FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      with:
        name: compile-log
        path: openwrt/compile.log
        if-no-files-found: warn

    - name: 8. 上传固件产物
      uses: actions/upload-artifact@v4
      env:
        FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      with:
        name: K2P-A2-Passwall-Slim
        path: openwrt/bin/targets/**/*.bin
        if-no-files-found: ignore
